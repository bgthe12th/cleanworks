<%= form_with(model: income, local: true) do |form| %>
  <% if income.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(income.errors.count, "error") %> prohibited this income from being saved:</h2>

      <ul>
      <% income.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :income_source %>
    <%= form.text_field :income_source %>
  </div>

  <div class="field">
    <%= form.label :income_amount %>
    <%= form.text_field :income_amount %>
  </div>

  <div class="field">
    <%= form.label :income_date %>
    <%= form.date_select :income_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
